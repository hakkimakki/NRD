# Private config options for can sample app

# Copyright (c) 2018 Alexander Wachter
# SPDX-License-Identifier: Apache-2.0

mainmenu "Controller Area Network sample application"

config GSM_PPP_AUTOSTART
	bool "Auto-start PPP at boot"
	default y
	help
	  This setting lets driver connect to network and initialize PPP at
	  boot. Unselect this if you want to run application code before
	  modem connects to network. See contents of "drivers/gsm_ppp.h"
	  to get an idea of the API.

config MODEM_GSM_HAS_SIMPIN
	bool "SIM Card PIN"
	default n
	help
	  If the GSM Modem is secured by a SIM PIN

config MODEM_GSM_SIMPIN
	string "SIM Card PIN"
	default "0000"
	help
	  If the GSM Modem is secured by a SIM PIN enter it as a string

source "Kconfig.zephyr"
